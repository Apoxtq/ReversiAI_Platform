# v0.8.0 - 性能验证与实验数据版

|||||
|---|---|---|---|
|**版本**|v0.8.0 (性能验证版)|
|**计划日期**|2026年3月中旬|
|**预计周期**|2周 (10-15工作日)|
|**参考项目**|Egaroucid, Reversi(Java), edax-reversi|
|**代码量**|~800行|
|**优先级**|P0 - 毕业必需|

---

## 1. 项目背景与学术对齐

### 1.1 学术背景

**对应Reversi_Proposal.md章节**:
- **8.3.1 Primary Performance Measure**: Head-to-head基准测试
- **8.3.1 Statistical Rigour**: Wilcoxon signed-rank测试
- **Essential Requirements**: 所有性能指标验证
- **毕业设计评分**: Merit/Distinction必需数据

### 1.2 参考项目分析

| 参考项目 | 优先级 | 借鉴内容 | 参考文件 |
|---------|--------|---------|---------|
| **Egaroucid** | ⭐⭐⭐⭐⭐ | 性能基准测试、PositionSuite、搜索统计 | `src/tools/benchmark/`, `src/engine/search.hpp` |
| **Reversi(Java)** | ⭐⭐⭐⭐⭐ | Minimax/评估函数、胜率测试 | `src/player/ai/Minimax.java` |
| **edax-reversi** | ⭐⭐⭐⭐ | 标准测试位置套件 | `src/problem.c` |
| **alpha-zero-general** | ⭐⭐⭐ | 统计显著性分析 | `Arena.py`, `Coach.py` |

### 1.3 详细参考代码

#### Reversi_Proposal.md 性能指标要求 (8.3.2)

| 指标 | 目标 | 硬件基准 |
|------|------|---------|
| Bitboard翻转速度 | ≥100M flips/sec | i7-1165G7 |
| Minimax吞吐量 | ≥2.0M nodes/sec | 深度6, 简单评估 |
| MCTS仿真率 | ≥200K sims/sec | Random策略 |
| LAN稳定性 | 10/10局 0断连 | <10ms延迟 |

#### Reversi_Proposal.md 胜率要求 (8.3.1)

| 对战组合 | 目标胜率 | 测试局数 | 统计要求 |
|----------|---------|---------|---------|
| Minimax-6 vs Random | ≥90% | 100局 | p<0.05 |
| MCTS vs Minimax-4 | ≥70% | 50局 | - |
| 优化算法 vs 基线 | 显著提升 | 50局 | Wilcoxon p<0.05 |

#### Reversi(Java) Minimax实现参考

**项目位置**: `OtherProjects/Reversi/src/player/ai/Minimax.java`

```java
public int minimax(Board board, int depth, int alpha, int beta, boolean maximizingPlayer) {
    if (depth == 0 || board.isGameOver()) {
        return evaluate(board);
    }
    
    List<Integer> moves = board.getValidMoves();
    if (moves.isEmpty()) {
        return evaluate(board);
    }
    
    // 按评估值排序走法 (简单走法排序)
    moves.sort((a, b) -> {
        Board temp = board.clone();
        temp.makeMove(a);
        int scoreA = evaluate(temp);
        temp.undoMove(a);
        temp.makeMove(b);
        int scoreB = evaluate(temp);
        return Integer.compare(scoreB, scoreA);
    });
    
    if (maximizingPlayer) {
        int maxEval = Integer.MIN_VALUE;
        for (int move : moves) {
            board.makeMove(move);
            int eval = minimax(board, depth - 1, alpha, beta, false);
            board.undoMove(move);
            maxEval = Math.max(maxEval, eval);
            alpha = Math.max(alpha, eval);
            if (beta <= alpha) break; // Alpha-Beta剪枝
        }
        return maxEval;
    } else {
        int minEval = Integer.MAX_VALUE;
        for (int move : moves) {
            board.makeMove(move);
            int eval = minimax(board, depth - 1, alpha, beta, true);
            board.undoMove(move);
            minEval = Math.min(minEval, eval);
            beta = Math.min(beta, eval);
            if (beta <= alpha) break;
        }
        return minEval;
    }
}
```

#### edax-reversi 标准测试位置

**项目位置**: `OtherProjects/edax-reversi/src/problem.c`

```c
// 标准测试位置结构
struct problem {
    int board[64];      // 棋盘状态
    int player;         // 当前玩家
    int best_move;      // 最佳走法
    int depth;          // 搜索深度
    int expected_score; // 期望分数
};

// 加载测试位置套件
void load_problem_set(struct problem* problems, int* count);
```

### 1.4 参考文件详细路径

| 功能 | 参考项目 | 详细路径 |
|------|---------|---------|
| 性能基准测试 | Egaroucid | `src/tools/benchmark/` |
| 搜索统计 | Egaroucid | `src/engine/search.hpp` |
| Minimax实现 | Reversi(Java) | `src/player/ai/Minimax.java` |
| 评估函数 | Reversi(Java) | `src/player/ai/StaticEvaluator.java` |
| 测试位置套件 | edax-reversi | `src/problem.c` |
| 统计分析 | alpha-zero-general | `Arena.py`, `Coach.py` |

---

## 2. 功能需求

### 2.1 性能基准测试模块 (P0)

#### 2.1.1 Bitboard性能测试

**功能描述**: 测量位棋盘操作的极限性能

```cpp
// Benchmark/BitboardBenchmark.h

#pragma once

#include <QObject>
#include <QVector>
#include <chrono>

namespace Reversi {

class BitboardBenchmark : public QObject {
    Q_OBJECT

public:
    explicit BitboardBenchmark(QObject* parent = nullptr);
    
    // 运行所有Bitboard基准测试
    QJsonObject runAllBenchmarks();
    
    // 翻转性能测试
    double measureFlipPerformance(int iterations = 10000000);
    
    // 移动生成性能测试
    double measureMoveGenerationPerformance(int iterations = 1000000);
    
    // 合法性检查性能测试
    double measureLegalMovePerformance(int iterations = 1000000);
    
    // 棋盘复制性能测试
    double measureBoardCopyPerformance(int iterations = 1000000);
    
signals:
    void benchmarkProgress(int percent, const QString& currentTest);
    void benchmarkCompleted(const QJsonObject& results);

private:
    void warmUp();
    double calculateStatistics(const QVector<double>& samples);
    QJsonObject createResult(const QString& name, double value, const QString& unit);
};

} // namespace Reversi
```

**验收标准**:
| 指标 | 目标 | 测试方法 |
|------|------|---------|
| 翻转速度 | ≥100M flips/sec | 10M次翻转计时 |
| 移动生成 | ≥50M moves/sec | 1M次生成计时 |
| 合法性检查 | ≥100M checks/sec | 1M次检查计时 |

#### 2.1.2 AI搜索性能测试

**功能描述**: 测量AI算法的搜索吞吐量

```cpp
// Benchmark/AIBenchmark.h

#pragma once

#include <QObject>
#include "ai/MinimaxAI.h"
#include "ai/MCTS.h"

namespace Reversi {

class AIBenchmark : public QObject {
    Q_OBJECT

public:
    explicit AIBenchmark(QObject* parent = nullptr);
    
    // Minimax性能测试
    QJsonObject benchmarkMinimax(const QString& position, int depth);
    double measureMinimaxThroughput(int depth, int timeLimitMs);
    
    // MCTS性能测试
    QJsonObject benchmarkMCTS(const QString& position, int simulations);
    double measureMCTSSimulationRate(int timeLimitMs);
    
    // 混合测试
    QJsonObject runFullAIBenchmark();
    
signals:
    void benchmarkProgress(int percent, const QString& currentTest);
    void benchmarkCompleted(const QJsonObject& results);

private:
    // 测试位置 (标准开局)
    static const char* TEST_POSITIONS_[];
    static const int NUM_TEST_POSITIONS = 8;
    
    double calculateNodesPerSecond(int64_t nodes, double timeMs);
};

} // namespace Reversi
```

**验收标准**:
| 指标 | 目标 | 测试方法 |
|------|------|---------|
| Minimax-6 吞吐量 | ≥2.0M nodes/sec | 多次搜索计时 |
| Minimax-8 吞吐量 | ≥1.0M nodes/sec | 多次搜索计时 |
| MCTS仿真率 | ≥200K sims/sec | 10秒仿真计时 |
| 搜索深度-6 | ≤5秒 | 10个位置平均 |

### 2.2 胜率验证模块 (P0)

#### 2.2.1 Head-to-Head对战引擎

**功能描述**: 系统性测试AI对战结果

```cpp
// Benchmark/HeadToHeadEngine.h

#pragma once

#include <QObject>
#include <QVector>
#include "ai/AIStrategy.h"
#include "core/Board.h"

namespace Reversi {

struct MatchResult {
    QString player1;
    QString player2;
    int player1Wins;
    int player2Wins;
    int draws;
    int totalGames;
    double player1WinRate;
    double player2WinRate;
    double drawRate;
    double confidenceInterval95[2];  // 95%置信区间
    double pValue;                  // Wilcoxon p值
};

class HeadToHeadEngine : public QObject {
    Q_OBJECT

public:
    explicit HeadToHeadEngine(QObject* parent = nullptr);
    
    // 运行对战
    MatchResult runMatch(AIStrategy* player1, 
                       AIStrategy* player2,
                       int numGames,
                       bool alternateFirstPlayer = true,
                       const QString& opening = "standard");
    
    // 运行系列对战
    QVector<MatchResult> runTournament(
        const QVector<AIStrategy*>& players,
        int gamesPerMatch
    );
    
    // 统计显著性检验
    double calculateWilcoxonPValue(const QVector<int>& sample1, 
                                  const QVector<int>& sample2);
    
signals:
    void matchProgress(int current, int total, const QString& status);
    void matchCompleted(const MatchResult& result);

private:
    GameResult playGame(AIStrategy* black, AIStrategy* white);
    double calculateWinRate(int wins, int total);
    QVector<double> calculateBootstrapCI(const QVector<int>& results, int iterations);
};

} // namespace Reversi
```

#### 2.2.2 胜率测试套件

**验收标准**:

| 对战组合 | 目标胜率 | 测试局数 | 显著性 |
|----------|---------|---------|--------|
| Minimax-6 vs Random | ≥90% | 100局 | p<0.05 |
| Minimax-8 vs Random | ≥95% | 100局 | p<0.05 |
| Minimax-6 vs MCTS-1000 | ≥55% | 50局 | - |
| MCTS-5000 vs Minimax-4 | ≥70% | 50局 | - |
| MCTS vs Random | ≥99% | 50局 | p<0.05 |

**统计要求**:
- 交替先手（黑/白）
- 95%置信区间
- Wilcoxon signed-rank检验（配对数据）
- 所有数据可重现

### 2.3 网络稳定性测试 (P1)

#### 2.3.1 网络对战稳定性

**功能描述**: 测试LAN网络对战的稳定性

```cpp
// Benchmark/NetworkBenchmark.h

#pragma once

#include <QObject>
#include "network/NetworkManager.h"

namespace Reversi {

struct NetworkStabilityResult {
    int totalGames;
    int successfulGames;
    int disconnectedGames;
    int reconnectionSuccess;
    double successRate;
    double averageLatency;
    double maxLatency;
    QVector<double> latencySamples;
};

class NetworkBenchmark : public QObject {
    Q_OBJECT

public:
    explicit NetworkBenchmark(QObject* parent = nullptr);
    
    // 运行网络稳定性测试
    NetworkStabilityResult runStabilityTest(
        const QString& hostAddress,
        int numGames
    );
    
    // 延迟测试
    QJsonObject measureLatency(const QString& hostAddress, int count);
    
signals:
    void testProgress(int current, int total);
    void testCompleted(const NetworkStabilityResult& result);

private:
    bool playNetworkGame(const QString& host);
    NetworkStabilityResult analyzeResults();
};

} // namespace Reversi
```

**验收标准**:
| 指标 | 目标 | 测试方法 |
|------|------|---------|
| 10局0断连 | 100%成功率 | 10局LAN对战 |
| 平均延迟 | <50ms | 100次PING |
| 最大延迟 | <200ms | 100次PING |

---

## 3. 实验数据收集

### 3.1 数据导出格式

```cpp
// Benchmark/DataExporter.h

#pragma once

#include <QJsonDocument>
#include <QJsonObject>
#include <QString>

namespace Reversi {

class DataExporter {
public:
    // 导出为JSON
    static bool exportToJson(const QJsonObject& data, const QString& filepath);
    
    // 导出为CSV
    static bool exportToCsv(const QVector<QVector<QVariant>>& data, 
                           const QString& filepath);
    
    // 导出为Markdown表格
    static bool exportToMarkdown(const QJsonObject& data, 
                                const QString& filepath);
    
    // 生成完整实验报告
    static bool generateExperimentReport(
        const QString& outputDir,
        const QJsonObject& benchmarkResults,
        const QVector<QJsonObject>& matchResults,
        const QJsonObject& networkResults
    );
};

} // namespace Reversi
```

### 3.2 数据结构

```json
{
  "experiment_info": {
    "date": "2026-03-15",
    "version": "v0.8.0",
    "platform": "Windows x64",
    "compiler": "MSVC 19.0",
    "cpu": "Intel Core i7-10700"
  },
  "bitboard_benchmark": {
    "flip_speed": {
      "value": 125.5,
      "unit": "M flips/sec",
      "iterations": 10000000
    },
    "move_generation": {
      "value": 68.3,
      "unit": "M moves/sec"
    }
  },
  "ai_benchmark": {
    "minimax6": {
      "throughput": 2.3,
      "unit": "M nodes/sec",
      "average_time": 4.2,
      "unit_time": "seconds"
    },
    "mcts": {
      "simulation_rate": 245.0,
      "unit": "K sims/sec"
    }
  },
  "head_to_head": {
    "minimax6_vs_random": {
      "wins": 94,
      "draws": 2,
      "losses": 4,
      "win_rate": 0.94,
      "confidence_interval": [0.89, 0.97],
      "p_value": 0.0001
    }
  },
  "network_stability": {
    "total_games": 10,
    "successful_games": 10,
    "success_rate": 1.0,
    "average_latency_ms": 35.2
  }
}
```

---

## 4. 测试计划

### 4.1 性能测试

| 测试项 | 目标 | 方法 |
|--------|------|------|
| Bitboard翻转 | ≥100M/s | 10M次循环计时 |
| Minimax吞吐量 | ≥2.0M/s | 搜索节点计数 |
| MCTS仿真率 | ≥200K/s | 仿真次数/时间 |
| 内存使用 | <200MB | 进程监控 |

### 4.2 胜率测试

| 测试项 | 目标 | 局数 | 方法 |
|--------|------|------|------|
| Minimax-6 vs Random | ≥90% | 100 | 交替先手 |
| Minimax-8 vs Random | ≥95% | 100 | 交替先手 |
| MCTS vs Random | ≥99% | 50 | 交替先手 |
| Minimax-6 vs MCTS | ≥55% | 50 | 交替先手 |

### 4.3 稳定性测试

| 测试项 | 目标 | 方法 |
|--------|------|------|
| 10局LAN对战 | 0断连 | 连续对战 |
| UI响应 | <50ms | 事件计时 |
| 长时间运行 | 无内存泄漏 | 1小时监测 |

---

## 5. 时间规划

```
v0.8.0 开发周期 (2周)
│
├── Week 1
│   ├── Day 1-2: Bitboard基准测试实现
│   ├── Day 3-4: AI性能基准测试实现
│   ├── Day 5: Head-to-Head引擎完善
│   └── Day 6-7: 单元测试 + 初步验证
│
└── Week 2
    ├── Day 8-9: 胜率测试运行 (关键!)
    ├── Day 10: 网络稳定性测试
    ├── Day 11: 数据导出 + 报告生成
    └── Day 12: 结果分析 + 文档
```

---

## 6. 风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 性能未达标 | 高 | 预留优化时间 |
| 胜率不稳定 | 中 | 增加测试局数 |
| 统计不显著 | 中 | 使用标准测试位置 |
| 测试耗时过长 | 中 | 并行测试 + 缓存结果 |

---

## 7. 交付物清单

- [ ] Bitboard性能测试工具
- [ ] AI性能基准测试工具
- [ ] Head-to-Head对战引擎
- [ ] 网络稳定性测试工具
- [ ] 实验数据导出模块
- [ ] 性能验证报告 (JSON/Markdown)
- [ ] 统计显著性分析结果
- [ ] 完整实验数据文件

---

**版本状态**: 设计完成，准备开发

**预计完成时间**: 2026年3月中旬

**前置依赖**: v0.7.0 (AI优化版)

**后续**: v0.9.0 或直接 v1.0.0
