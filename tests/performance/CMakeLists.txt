cmake_minimum_required(VERSION 3.16)

project(performance_tests)

# Performance tests are disabled because source files were moved to tests/experiments
# This file is kept for CMake compatibility but no targets are built

# To run tests, use the files in tests/experiments/ directory